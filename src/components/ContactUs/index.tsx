type ContactUsProps = {
  title: string,
  subtitle: string,
  fullName: string,
  message: string,
  email: string
}

const ContactUs = () => {
  return (
    <div className="bg-primary">
      <h2 className='text-secondary'>{}</h2>
    </div>
  )
}

export default ContactUs